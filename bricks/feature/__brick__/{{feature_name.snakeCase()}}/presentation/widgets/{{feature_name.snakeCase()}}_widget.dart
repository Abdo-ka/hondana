import 'package:flutter/material.dart';

/// Primary content widget for the {{feature_name.titleCase()}} feature, shown once
/// the Bloc reports success. Break large screens into more widget classes here.
class {{feature_name.pascalCase()}}Widget extends StatelessWidget {
  const {{feature_name.pascalCase()}}Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
